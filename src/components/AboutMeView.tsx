import React from 'react';
import { Box, Typography } from '@mui/material';

function AboutMeView() {
    return (
        <Box height={600} marginTop={10}>
            <Typography variant="h1">AboutMe</Typography>            
        </Box>
    );
}

export default AboutMeView;